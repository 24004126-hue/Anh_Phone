{
  "name": "phonestore-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend/phonestore-client run build",
    "dev": "npm --prefix frontend/phonestore-client run dev"
  }
}
